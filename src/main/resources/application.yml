server:
  port: 9010

spring:
  data:
    redis:
      port: 6379
      host: localhost

---
spring:
  config:
    activate:
      on-profile: test
  data:
    redis:
      port: 63790
      host: localhost